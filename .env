# Base runtime configuration (commit-safe; secrets belong in vaults or the host environment)

APP_ENV=dev
APP_SECRET=change-this-secret
DEFAULT_URI=http://localhost

# Database connection string (see doctrine.yaml for serverVersion overrides)
DATABASE_URL="mysql://root:root@127.0.0.1:3306/patchstash?serverVersion=8.0.33&charset=utf8mb4"
# Examples to switch quickly:
# DATABASE_URL="sqlite:///%kernel.project_dir%/var/patchstash_dev.db"
# DATABASE_URL="mysql://app:pass@db:3306/patchstash?serverVersion=10.11.2-MariaDB&charset=utf8mb4"

# Async transport (Doctrine queue by default)
MESSENGER_TRANSPORT_DSN=doctrine://default?auto_setup=0
# MESSENGER_TRANSPORT_DSN=redis://localhost:6379/messages
# MESSENGER_TRANSPORT_DSN=amqp://guest:guest@localhost:5672/%2f/messages

# Mail transport (configure secrets in .env.local only)
# Example for ProtonMail SMTP (App Password/token required):
# MAILER_DSN="smtp://contact%40yacrb.com:4NZKSQ8D9GTX6TMD@smtp.protonmail.ch:587?encryption=tls&auth_mode=login"
MAILER_DSN=null://null
MAILER_FROM="Patchstash <contact@yacrb.com>"
