{% extends 'admin/base.html.twig' %}

{% block title %}Patch Cycles · Admin{% endblock %}

{% block admin_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Operations</p>
        <h1>Patch cycles</h1>
        <p class="text-muted">Windows, deadlines, and rollout tracking.</p>
    </div>
    <div class="actions">
        <a class="btn primary" href="{{ path('app_patch_cycle_index') }}">Open patch cycles</a>
    </div>
</div>

<div class="admin-panel">
    <p class="text-muted">Use the Patch Cycles list to view CVSS context, applied dates, and linked vulnerabilities.</p>
</div>
{% endblock %}
