{% extends 'admin/base.html.twig' %}

{% block title %}New User{% endblock %}

{% block admin_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Create</p>
        <h1>New user</h1>
        <p class="text-muted">Provision access and set role.</p>
    </div>
    <div class="actions">
        <a class="btn ghost" href="{{ path('app_admin_users') }}">Back</a>
    </div>
</div>

{{ include('admin/user/_form.html.twig') }}
{% endblock %}
