<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <title>{% block title %}Welcome!{% endblock %}</title>
        <link rel="icon" href="data:image/svg+xml,<svg xmlns=%22http://www.w3.org/2000/svg%22 viewBox=%220 0 128 128%22><text y=%221.2em%22 font-size=%2296%22>⚫️</text><text y=%221.3em%22 x=%220.2em%22 font-size=%2276%22 fill=%22%23fff%22>sf</text></svg>">
        {% block stylesheets %}
            <style>
                body.layout {
                    margin: 0;
                    font-family: var(--font-sans);
                    background:
                        radial-gradient(circle at 12% 18%, rgba(168, 85, 247, 0.18), transparent 38%),
                        radial-gradient(circle at 88% 10%, rgba(56, 189, 248, 0.2), transparent 42%),
                        var(--app-bg-strong);
                    color: var(--app-text);
                    min-height: 100vh;
                }
                .shell { max-width: 1200px; margin: 0 auto; padding: 0 1.5rem; }
                .topnav {
                    display: flex;
                    justify-content: space-between;
                    align-items: center;
                    padding: 0.85rem 1.1rem;
                    background: rgba(10, 16, 32, 0.7);
                    border-radius: 16px;
                    border: 1px solid var(--app-border);
                    box-shadow: var(--app-shadow);
                    backdrop-filter: blur(12px);
                }
                .topnav a {
                    color: var(--app-text);
                    text-decoration: none;
                    margin-left: 0.4rem;
                    padding: 0.35rem 0.75rem;
                    border-radius: 999px;
                    transition: background 0.2s ease, color 0.2s ease, box-shadow 0.2s ease;
                }
                .topnav a:hover {
                    background: rgba(56, 189, 248, 0.15);
                    color: #f8fafc;
                    box-shadow: 0 8px 20px rgba(56, 189, 248, 0.2);
                }
                .topnav .brand { font-weight: 700; letter-spacing: 0.08em; text-transform: uppercase; }
                .nav-links { display: flex; align-items: center; gap: 0.4rem; }
                .muted { color: var(--app-muted); }
                .flash-message {
                    border-radius: 14px;
                    box-shadow: var(--app-shadow);
                    backdrop-filter: blur(8px);
                    font-weight: 500;
                }
                main.shell {
                    padding: 1.5rem 0 3rem;
                    animation: fadeUp 0.35s ease both;
                }
                @keyframes fadeUp {
                    from { opacity: 0; transform: translateY(8px); }
                    to { opacity: 1; transform: translateY(0); }
                }
                @media (max-width: 720px) {
                    .topnav {
                        flex-direction: column;
                        align-items: flex-start;
                        gap: 0.6rem;
                    }
                    .nav-links { flex-wrap: wrap; }
                    .topnav a { margin-left: 0; }
                }
                @media (prefers-reduced-motion: reduce) {
                    main.shell { animation: none; }
                }
            </style>
        {% endblock %}

        {% block javascripts %}
            {% block importmap %}{{ importmap('app') }}{% endblock %}
        {% endblock %}
    </head>
    <body class="{% block body_class %}layout{% endblock %}">
        {% block header %}
            <header class="shell">
                <nav class="topnav">
                    <a href="{{ path('app_dashboard') }}" class="brand">Patchstash</a>
                    <div class="nav-links">
                        {% if is_granted('ROLE_ADMIN') %}
                            <a href="{{ path('app_admin_dashboard') }}">Admin</a>
                        {% endif %}
                        {% if app.user %}
                            <span class="muted">Hi {{ app.user.username }}</span>
                            <a href="{{ path('app_logout') }}">Logout</a>
                        {% else %}
                            <a href="{{ path('app_login') }}">Login</a>
                        {% endif %}
                    </div>
                </nav>
            </header>
        {% endblock %}
        
        {# Display flash messages #}
        {% for type, messages in app.flashes %}
            {% for message in messages %}
                <div class="flash-message flash-{{ type }} shell" style="margin-top: 1rem; padding: 1rem; border-radius: 0.5rem; {% if type == 'error' %}background: #7f1d1d; color: #fee2e2; border-left: 4px solid #dc2626;{% elseif type == 'success' %}background: #15803d; color: #dcfce7; border-left: 4px solid #22c55e;{% elseif type == 'warning' %}background: #7c2d12; color: #fed7aa; border-left: 4px solid #ea580c;{% else %}background: #1e40af; color: #dbeafe; border-left: 4px solid #3b82f6;{% endif %}">
                    {{ message }}
                </div>
            {% endfor %}
        {% endfor %}
        <main class="{% block main_class %}shell{% endblock %}">
            {% block body %}{% endblock %}
        </main>
    </body>
</html>
