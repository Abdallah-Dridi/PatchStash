{% extends 'admin/base.html.twig' %}

{% block title %}New Module{% endblock %}

{% block admin_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Create</p>
        <h1>New module</h1>
        <p class="text-muted">Add a module to organize assets and patch cycles.</p>
    </div>
    <div class="actions">
        <a class="btn ghost" href="{{ path('app_admin_modules') }}">Back</a>
    </div>
</div>

{{ include('admin/module/_form.html.twig') }}
{% endblock %}
