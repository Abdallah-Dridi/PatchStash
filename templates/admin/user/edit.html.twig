{% extends 'base.html.twig' %}

{% block title %}Edit User · Admin{% endblock %}

{% block body %}
    <div class="shell" style="padding-top:2rem; max-width:720px;">
        <p class="eyebrow">Directory</p>
        <h1>Edit user</h1>
        <p class="text-muted">Update account details. Leave password blank to keep current.</p>

        {{ include('admin/user/_form.html.twig', {'button_label': 'Update', 'form': form}) }}

        <div style="margin-top:1rem; display:flex; justify-content:space-between; align-items:center;">
            <a href="{{ path('app_admin_users') }}">← Back to list</a>
            {{ include('admin/user/_delete_form.html.twig') }}
        </div>
    </div>
{% endblock %}
