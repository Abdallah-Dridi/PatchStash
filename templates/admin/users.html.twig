{% extends 'admin/base.html.twig' %}

{% block title %}Users · Admin{% endblock %}

{% block admin_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Directory</p>
        <h1>Users</h1>
        <p class="text-muted">Manage platform accounts and roles.</p>
    </div>
    <div class="actions">
        <a class="btn primary" href="{{ path('app_admin_users') }}">Open users</a>
    </div>
</div>

<div class="admin-panel">
    <p class="text-muted">Use the Users section to invite, edit, or remove operators.</p>
</div>
{% endblock %}
