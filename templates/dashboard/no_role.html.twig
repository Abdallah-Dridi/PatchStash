{% extends 'base.html.twig' %}

{% block title %}No Role Assigned · Dashboard{% endblock %}

{% block stylesheets %}
    {{ parent() }}
    <style>
        .no-role-hero {
            max-width: 620px;
            margin: 5rem auto;
            text-align: center;
            padding: 3rem;
            border-radius: 18px;
            background: linear-gradient(135deg, rgba(239, 68, 68, 0.1), rgba(251, 146, 60, 0.08));
            border: 1px solid rgba(248, 113, 113, 0.3);
            box-shadow: 0 20px 50px rgba(15, 23, 42, 0.12);
        }
        .no-role-hero .icon {
            font-size: 4rem;
            margin-bottom: 1rem;
        }
        .no-role-hero h1 {
            margin: 0 0 0.8rem;
            color: #1f2937;
        }
        .no-role-hero p {
            margin: 0 0 1.2rem;
            color: #6b7280;
            line-height: 1.6;
        }
        .alert-box {
            background: #fef2f2;
            border-left: 4px solid #ef4444;
            padding: 1.2rem;
            border-radius: 8px;
            margin: 1.5rem 0;
            color: #7f1d1d;
            text-align: left;
        }
        .alert-box strong {
            display: block;
            margin-bottom: 0.5rem;
        }
        .contact-info {
            background: #f3f4f6;
            padding: 1.2rem;
            border-radius: 8px;
            margin-top: 1.5rem;
            font-size: 0.95rem;
        }
        .contact-info p {
            margin: 0.4rem 0;
        }
        .btn-secondary {
            display: inline-block;
            padding: 0.75rem 1.5rem;
            margin-top: 1.5rem;
            background: #6b7280;
            color: #fff;
            border-radius: 8px;
            text-decoration: none;
            font-weight: 600;
            transition: all 0.2s ease;
        }
        .btn-secondary:hover {
            background: #4b5563;
        }
    </style>
{% endblock %}

{% block body %}
    <div class="no-role-hero">
        <div class="icon">⚠️</div>
        <h1>No Role Assigned</h1>
        <p>Your account has been created, but an administrator hasn't assigned you a role yet.</p>
        
        <div class="alert-box">
            <strong>What does this mean?</strong>
            <p>Without a role assignment, you cannot access the project management features. A role defines your permissions and responsibilities within Patchstash.</p>
        </div>

        <div class="contact-info">
            <p><strong>Please contact your administrator or technical support team to:</strong></p>
            <p>✓ Assign your role (Project Manager, Operator, or Auditor)</p>
            <p>✓ Grant access to your assigned projects</p>
        </div>

        <a href="{{ path('app_logout') }}" class="btn-secondary">Logout</a>
    </div>
{% endblock %}
