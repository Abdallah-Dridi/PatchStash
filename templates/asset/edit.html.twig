{% extends 'admin/base.html.twig' %}

{% block title %}Edit Asset{% endblock %}

{% block admin_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Edit</p>
        <h1>Update asset</h1>
        <p class="text-muted">Adjust environment, module, or status.</p>
    </div>
    <div class="actions">
        <a class="btn ghost" href="{{ path('app_asset_index') }}">Back</a>
    </div>
</div>

{{ include('asset/_form.html.twig', {'button_label': 'Update asset'}) }}
{% endblock %}
