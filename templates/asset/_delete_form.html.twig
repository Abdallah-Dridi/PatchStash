<form method="post" action="{{ path('app_asset_delete', {'id': asset.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');" style="margin-top:1rem;">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ asset.id) }}">
    <button class="btn ghost">Delete</button>
</form>
