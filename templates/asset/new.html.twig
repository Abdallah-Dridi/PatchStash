{% extends 'admin/base.html.twig' %}

{% block title %}New Asset{% endblock %}

{% block admin_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Create</p>
        <h1>New asset</h1>
        <p class="text-muted">Register a system, service, or component.</p>
    </div>
    <div class="actions">
        <a class="btn ghost" href="{{ path('app_asset_index') }}">Back</a>
    </div>
</div>

{{ include('asset/_form.html.twig') }}
{% endblock %}
