{% extends 'user/base.html.twig' %}

{% block title %}Edit Module{% endblock %}

{% block user_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Edit</p>
        <h1>Update module</h1>
        <p class="text-muted">Change project linkage or status.</p>
    </div>
    <div class="actions">
        <a class="btn ghost" href="{{ path('app_module_index') }}">Back</a>
    </div>
</div>

{{ include('module/_form.html.twig', {'button_label': 'Update module'}) }}
{% endblock %}
