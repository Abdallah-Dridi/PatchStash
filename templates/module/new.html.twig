{% extends 'admin/base.html.twig' %}

{% block title %}New Module{% endblock %}

{% block admin_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Create</p>
        <h1>New module</h1>
        <p class="text-muted">Attach a module to a project.</p>
    </div>
    <div class="actions">
        <a class="btn ghost" href="{{ path('app_module_index') }}">Back</a>
    </div>
</div>

{{ include('module/_form.html.twig') }}
{% endblock %}
