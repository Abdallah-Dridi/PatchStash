{% extends 'admin/base.html.twig' %}

{% block title %}Edit Project{% endblock %}

{% block admin_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Edit</p>
        <h1>Update project</h1>
        <p class="text-muted">Adjust status, owner, or description.</p>
    </div>
    <div class="actions">
        <a class="btn ghost" href="{{ path('app_project_index') }}">Back</a>
    </div>
</div>

{{ include('project/_form.html.twig', {'button_label': 'Update project'}) }}
{% endblock %}
