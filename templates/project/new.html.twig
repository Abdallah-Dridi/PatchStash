{% extends 'user/base.html.twig' %}

{% block title %}New Project{% endblock %}

{% block user_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Create</p>
        <h1>New project</h1>
        <p class="text-muted">Define scope, owner, and status.</p>
    </div>
    <div class="actions">
        <a class="btn ghost" href="{{ path('app_project_index') }}">Back</a>
    </div>
</div>

{{ include('project/_form.html.twig') }}
{% endblock %}
