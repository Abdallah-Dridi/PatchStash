{% extends 'admin/base.html.twig' %}

{% block title %}Edit Patch Cycle{% endblock %}

{% block admin_content %}
<div class="page-head">
    <div>
        <p class="eyebrow">Edit</p>
        <h1>Update patch cycle</h1>
        <p class="text-muted">Adjust deadlines, CVSS score, or status.</p>
    </div>
    <div class="actions">
        <a class="btn ghost" href="{{ path('app_patch_cycle_index') }}">Back</a>
    </div>
</div>

{{ include('patch_cycle/_form.html.twig', {'button_label': 'Update cycle'}) }}
{% endblock %}
